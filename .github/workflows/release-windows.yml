name: Release Windows

on:
  workflow_call:
    inputs:
      version:
        description: Release version without the leading v
        required: true
        type: string
      artifact-retention-days:
        description: Number of days to retain uploaded workflow artifacts
        required: false
        type: number
        default: 30

defaults:
  run:
    shell: pwsh

permissions:
  contents: read

jobs:
  build:
    name: Build Windows MSVC binary
    runs-on: windows-latest
    env:
      TARGET_TRIPLE: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --locked --target $env:TARGET_TRIPLE

      - name: Verify release binary
        run: |
          $ErrorActionPreference = "Stop"
          $exe = "target\$env:TARGET_TRIPLE\release\envq.exe"
          if (-not (Test-Path $exe)) {
            throw "Missing release binary: $exe"
          }
          & $exe --version
          $signature = Get-AuthenticodeSignature $exe
          if ($signature.Status -eq "NotSigned") {
            Write-Host "Windows binary is currently unsigned; Authenticode signing is not configured yet."
          } else {
            $signature | Format-List
          }

      - name: Generate shell completions
        run: |
          $ErrorActionPreference = "Stop"
          $exe = "target\$env:TARGET_TRIPLE\release\envq.exe"
          New-Item -ItemType Directory -Force dist\completions | Out-Null
          & $exe completion bash | Set-Content -Encoding utf8NoBOM dist\completions\envq.bash
          & $exe completion zsh | Set-Content -Encoding utf8NoBOM dist\completions\_envq
          & $exe completion fish | Set-Content -Encoding utf8NoBOM dist\completions\envq.fish
          & $exe completion powershell | Set-Content -Encoding utf8NoBOM dist\completions\envq.ps1

      - name: Package release archive
        run: |
          $ErrorActionPreference = "Stop"
          $package = "envq-${{ inputs.version }}-$env:TARGET_TRIPLE"
          $packageDir = "dist\$package"
          New-Item -ItemType Directory -Force $packageDir | Out-Null
          Copy-Item "target\$env:TARGET_TRIPLE\release\envq.exe" "$packageDir\envq.exe"
          Copy-Item -Recurse dist\completions "$packageDir\completions"
          Copy-Item README.md, LICENSE, THIRD-PARTY-LICENSES.md $packageDir
          Compress-Archive -Path "$packageDir\*" -DestinationPath "dist\$package.zip"

      - name: Upload Windows archive
        uses: actions/upload-artifact@v7
        with:
          name: envq-windows-x86_64
          path: dist/envq-${{ inputs.version }}-x86_64-pc-windows-msvc.zip
          if-no-files-found: error
          retention-days: ${{ inputs['artifact-retention-days'] }}
